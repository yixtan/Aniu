export const stockApiKeys = {
  settings: ["stock-api-settings"] as const,
};

export const notificationKeys = {
  channels: ["notification-channels"] as const,
  deliveries: ["notification-deliveries"] as const,
};
